# Application handling of errors
# if Web application is running and an error occurs, we can handle it gracefully

# Cuase Internet, Server, Database errors etc.
# We can handle these errors gracefully by showing a user-friendly error message
# and logging the error for further analysis.

